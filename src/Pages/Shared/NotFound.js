import React from 'react';
import error from '../../media/images/error.png'

const NotFound = () => {
    return (
        <div>
            
            <img src={error} alt=''/>
        </div>
    );
};

export default NotFound;